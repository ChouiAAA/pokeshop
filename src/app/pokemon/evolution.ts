export class Evolutions{

  name: string;
  pokedexId: number;

    constructor(
      name: string,
      pokedexId: number,
    ){
      this.name = name;
      this.pokedexId = pokedexId;
    }

  }
